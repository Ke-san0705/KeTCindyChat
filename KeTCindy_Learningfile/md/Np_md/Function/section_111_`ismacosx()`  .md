# 平面の図形とグラフ  
## ファイル操作  
### マーキング  
#### `ismacosx()`  
`ismacosx()`  
macOSかどうかを返す  
返り値は`<bool>`型。  
  
## `iswindows()`  
Windowsかどうかを返す  
返り値は`<bool>`型。  
  
## `islinux()`  
Linuxかどうかを返す  
返り値は`<bool>`型。  
  
## その他
